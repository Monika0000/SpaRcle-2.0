#include "pch.h"
#include "UI.h"
#include <GL/freeglut.h>
#include <wingdi.h>

/*
void SpaRcle::Graphics::UI::RenderString3D(const char* string, Vector3 pos, Vector3 size, void* font, color const& rgb) {
	if (font == NULL)
		font = GLUT_BITMAP_TIMES_ROMAN_24;
	glColor3f(rgb.r, rgb.g, rgb.b);
	glRasterPos3f(pos.x, pos.y, pos.z);
	//glTranslatef(size.x, size.y, size.z);

	//glutStrokeString(GLUT_STROKE_ROMAN, (unsigned const char*)string);
	glutBitmapString(font, (unsigned const char*)string);
}
*/